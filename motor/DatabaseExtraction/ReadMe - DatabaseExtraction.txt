It's preferrable to use SQLitedatabase_import.m: it's faster and doesn't need to have XML files (that are painfully extracted from .dcd database).

XMLdatabase_import.m was built only because I haven't found SLQlite extractor (mksqlite-1.13).

Paste database.mat in motor.m directory